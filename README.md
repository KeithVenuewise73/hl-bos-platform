# TEMPORARY BRANCH - CONTENT REMOVED

This branch briefly carried a temporary HLVS discovery artifact (public GitHub
repository metadata) so that Postgres could fetch it across a network boundary
the sandbox could not cross. The corpus is now persisted in the production
database and the artifact has been removed.

This branch can be deleted. It contains no secrets and nothing depends on it.

