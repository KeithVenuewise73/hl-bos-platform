{
  "name": "@hl-bos/cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Herman Legacy Cloud — the hosted execution plane of Headquarters. Exposes only remotely-safe operations; it shells out to nothing.",
  "type": "module",
  "scripts": {
    "dev": "next dev --port 4100",
    "build": "next build",
    "start": "next start --port 4100",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .next dist *.tsbuildinfo"
  },
  "dependencies": {
    "@hl-bos/deploy": "workspace:*",
    "@hl-bos/lifecycle": "workspace:*",
    "@hl-bos/ui": "workspace:*",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "server-only": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
