{
  "name": "@hl-bos/ats-resume-optimizer",
  "version": "0.1.0",
  "private": true,
  "description": "ATS Resume Optimizer \u2014 compare a job posting against a master resume, see every requirement matched against real evidence, and generate a tailored, ATS-friendly resume in which every sentence is traceable to a source fact. Optimize aggressively, fabricate nothing.",
  "type": "module",
  "scripts": {
    "dev": "next dev --port 4600 --hostname 127.0.0.1",
    "build": "next build",
    "start": "next start --port 4600 --hostname 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rm -rf .next dist *.tsbuildinfo"
  },
  "dependencies": {
    "@hl-bos/ats-resume": "workspace:*",
    "@supabase/ssr": "catalog:",
    "@supabase/supabase-js": "catalog:",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "server-only": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
