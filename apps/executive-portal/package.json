{
  "name": "@hl-bos/executive-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Herman Legacy Executive Portal — a secure, READ-ONLY, cloud-deployable executive view over the Enterprise Catalog and Software Factory. Reuses @hl-bos/catalog. Contains NO shell/git/pnpm/filesystem command surface. Separate from apps/control-center (which stays localhost-only). Authenticated via HL-BOS identity (Supabase Auth); all actions are read-only.",
  "type": "module",
  "scripts": {
    "dev": "next dev --port 4300",
    "build": "next build",
    "start": "next start --port 4300",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rm -rf .next dist *.tsbuildinfo"
  },
  "dependencies": {
    "@hl-bos/catalog": "workspace:*",
    "@supabase/ssr": "catalog:",
    "@supabase/supabase-js": "catalog:",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "server-only": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
