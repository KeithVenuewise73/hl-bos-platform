{
  "name": "@hl-bos/lifecycle",
  "version": "0.1.0",
  "private": true,
  "description": "The Herman Legacy company lifecycle: the stages every company moves through — provision, operate, monitor, update, back up, retire — and the remote-safety class of each action. Shared by both planes so they agree on what may run where.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
