/**
 * @hl-bos/ui — the Herman Legacy shared shell.
 *
 * The design-system primitives and status vocabulary rendered by both planes of
 * Herman Legacy Cloud: the operator plane (Headquarters) and the hosted cloud
 * plane. One shell, so the two surfaces read as one product.
 *
 * Presentational and tenant-neutral by design. See README.md.
 */

export { type Health, DOT, LABEL } from "./status.js";
export { Card, Dot, Row, Empty } from "./components.js";
