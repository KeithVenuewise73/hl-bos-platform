{
  "$comment": "Live milestone state. Version-controlled so it is auditable, and updated by the AI engineer as work lands. The Control Center reads this -- it does not guess progress.",
  "milestone": "Platform Foundation v1.0 — Enterprise Knowledge Graph live in production",
  "phase": "The sale is two stages and both halves now exist. The AUDIT produces the opener -- what anyone can see of a shop's internet presence -- and the DISCOVERY CALL produces the proposal, because what a shop already RUNS (its phones, its appointment book, what it keeps on a regular) can only be learned by asking. Migration 0054 is APPLIED to HL-BOS Core, fingerprint-identical local and production on the first comparison (f015d345b71fac28f0ae26937a502af1), and the call screen at /shops/<id>/call writes through it as the tenant owner rather than as the superuser the transport connects as. Every answer is three-state -- yes, no, and NOBODY ASKED -- end to end, from the three buttons on the screen to the column in the database, because collapsing the last two is how a proposal tells an owner something nobody said. Shop pages remain LIVE at https://shops.hermanlegacydigital.com. 1,113 pgTAP assertions, 842 unit tests, 211 edge-function tests, 0 failing. THE HONEST STATE OF THE OFFER: the cross-reference yields exactly ONE capability deliverable today (Owned Website) against five on the roadmap, because eight of nine BarberOS modules are unbuilt; the SEO, funnel and agency half of the value proposition is not in the catalog at all; and no proposal document comes out of the call yet. Still no customer: no shop has the capability enabled, none has published a page, and nobody has been called.",
  "engineer": "Claude (AI engineer)",
  "startedOn": "2026-07-16",
  "tasks": [
    {
      "title": "Phase 2: identity, tenancy, permissions, audit",
      "done": true
    },
    {
      "title": "Phase 2 merged to main",
      "done": true
    },
    {
      "title": "Control Center: project status, one-click actions",
      "done": true
    },
    {
      "title": "Control Center: plain-English failure translation",
      "done": true
    },
    {
      "title": "Control Center: quality gates + approval queue",
      "done": true
    },
    {
      "title": "Control Center is the only entry point",
      "done": true
    },
    {
      "title": "Operating contract so future sessions honour it",
      "done": true
    },
    {
      "title": "Approve-and-merge from inside the console",
      "done": true
    },
    {
      "title": "Supabase status panel",
      "done": true
    },
    {
      "title": "In-console connection setup (no text editor, no terminal)",
      "done": true
    },
    {
      "title": "Enterprise Catalog activated in the console (browse, global search, asset relationships, completeness metrics)",
      "done": true
    },
    {
      "title": "Software Factory assembly engine (module registry, product compositions, SalonAI reference assembly, executive readiness) in the console",
      "done": true
    },
    {
      "title": "Herman Legacy Executive Portal — secure, read-only, cloud-deployable app (HL-BOS auth, 5 roles, no command surface); staging-ready, not yet deployed",
      "done": true
    },
    {
      "title": "HL-BTI v2 Business Transformation Intelligence engine (@hl-bos/transformation-intelligence): configurable scoring, evidence-gated impact/ROI, factory reuse, CEO-approval gating, government intelligence",
      "done": true
    },
    {
      "title": "Enterprise Application Registry + Executive Portal operationalization (CEO Home, Task Center, Global Search, Platform Status, grouped navigation) — read-only, governed, not yet deployed",
      "done": true
    },
    {
      "title": "Canonical Capability Library (Phase XI-1): one evidence-backed record unifying 6 capability registries, deterministic duplicate/reuse gate, read-only /capabilities view — in-code, no migration, not merged",
      "done": true
    },
    {
      "title": "HL-BTI v2 Business Transformation Intelligence engine (@hl-bos/transformation-intelligence) — configurable scoring, evidence-gated impact/ROI, Software Factory reuse, CEO-approval gating, HLVS + government-contracts intelligence; Executive Portal Transformation Intelligence + Government Contracts views over labelled sample input",
      "done": true
    },
    {
      "title": "Connect GitHub and Supabase (needs your tokens)",
      "done": false
    },
    {
      "title": "Knowledge Graph validated on a production-cut preview (XI-2J): full runtime authz/RLS/RPC/rollback; a real RPC bug was found, fixed, and covered by a regression test",
      "done": true
    },
    {
      "title": "Platform Foundation v1.0 merged to main via PR #18 (CI green: Validate, pgTAP 0001–0028 + graph execution tests, Migration checks, Deno, Secret scan)",
      "done": true
    },
    {
      "title": "Deploy Production — migration 0028 (Knowledge Graph) applied to canonical production (mvvtngiopdrgiedjmhfb) at exact repo version 20260731090000; 17/17 structural checks pass; RLS forced; RPCs fail-closed; lineage reconciled (XI-2L)",
      "done": true
    },
    {
      "title": "Grant graph permissions + publish the first production projection (145 nodes / 427 edges) — a separate CEO authorization; the graph is installed but dormant until then",
      "done": false
    },
    {
      "title": "HLVS V2-1 foundation merged to main via PR #21 (CI green: Validate, pgTAP incl. 22_venture_studio, Migration checks, Deno, Secret scan)",
      "done": true
    },
    {
      "title": "Deploy Production — migration 0029 (vstudio Venture Studio foundation) applied to canonical production (mvvtngiopdrgiedjmhfb); 6 tables, RLS forced, 5 permissions, 8 SECURITY DEFINER functions; purely additive; applied DDL byte-identical to the committed file (sha256 ee83df9…); recorded name matches, version timestamp differs (management-API stamp) and is reconciled by documentation (Option A)",
      "done": true
    },
    {
      "title": "Deploy Venture Studio app + expose vstudio to the API + choose VSTUDIO_TENANT_ID — a separate CEO authorization; migration is applied but the app is undeployed and the schema is not yet exposed until then",
      "done": false
    },
    {
      "title": "CEO Notebook (Workstream B1): executive intelligence workspace assembled on vstudio.notes (migration 0030 additive, UNAPPLIED), living intelligence objects composing evidence/reuse/factory/decisions, six permanent Intelligence Programs; pnpm check + build green (397 tests), pgTAP CI-verified; PR opened, not merged",
      "done": true
    },
    {
      "title": "Apply migration 0030 (CEO Notebook) to production — a separate CEO authorization; written and CI-verified but UNAPPLIED",
      "done": false
    },
    {
      "title": "HLVS Phase 2 (Steps 1-2): corpus-preservation baseline recorded and proven — 62,250 opportunities, 62,214 canonical URLs, 0 duplicates, original-36 fingerprint 3679f569f706e8db94d314e7de19bb05; supabase/checks/corpus-preservation.sql returns 14/14 pass against production",
      "done": true
    },
    {
      "title": "HLVS Phase 2 (Steps 3-5): additive intelligence schema (0033), Level-1 triage engine generated from tested TypeScript (0034), portfolio builder (0035); all 62,250 records scored; Logistics/Business Transformation/Sports/Outside-Core each populated to 100",
      "done": true
    },
    {
      "title": "HLVS Phase 2 (Step 6): Rising Opportunities populated — a second observation pass over 299 of 301 discovery queries produced 1,562 material movements against the 62,214-row baseline; score_rising() ranked all 1,562 over a 0.92-day measured window and the portfolio holds 100",
      "done": true
    },
    {
      "title": "HLVS Phase 2 (Steps 7-9): public pain engine (0037) with phrase verification against full issue bodies, deterministic clustering, and the HLVS UI — Executive Overview, five Top-100 routes, Rising, Discovery Universe, Sources & Settings",
      "done": true
    },
    {
      "title": "HLVS Phase 2 (CI feedback): the pgTAP suite ran for the first time and found real defects — nine intelligence gates whose refusal path was unreachable under test, and a source_url guard that accepted a tab. Migrations 0039 and 0040 fix both. Written and verified against a local Postgres 16 with all 40 migrations applied from empty: 801 assertions, 0 failures. UNAPPLIED to production pending CEO approval.",
      "done": false
    },
    {
      "title": "HLVS Phase 3 (A/B/H): source-neutral pain schema (0041), GitHub evidence backfill (0042) linking 124 of 439 signals to the corpus as evidence_source and never as solutions, capability intelligence (0043) reusing the @hl-bos/catalog vocabulary, Level-1 extraction across all 62,250 (0044) yielding 24,411 evidence-backed capability claims over 19,585 repositories, and the pain portfolio relabelled to name its population (0045). Reddit and every other non-GitHub source recorded as blocked by this environment's egress policy, verified by probe, not assumed.",
      "done": true
    },
    {
      "title": "HL Social Publishing Phase 1 (schema + workers + adapters): migration 0046 creates the `social` schema -- accounts, credentials, posts, post_targets, publish_attempts, media_assets -- with a human approval gate, FOR UPDATE SKIP LOCKED claiming, a trigger-enforced append-only attempt log, and per-target independence. Credentials are Vault REFERENCES only, and social.credentials has zero RLS policies and zero grants. TikTok is inbox-only with its own terminal status so no draft can read as live. APPLIED to canonical production 2026-08-26 with 0047 (forward-repair: search_path pinned on deny_attempt_mutation, caught by the advisor AFTER apply, not by the local suite -- a test now guards the whole schema against a recurrence). Advisor delta 35 -> 36, the one net-new finding being the intended policy-less credentials table. 909 pgTAP assertions and 26 edge tests, 0 failing. NOT YET CONNECTED to any real channel and the two edge functions are NOT deployed -- nothing has published to an audience.",
      "done": true
    },
    {
      "title": "Video Studio: a still image becomes a video, from inside the console. @hl-bos/video-studio finds the panels in a composite image by scoring every row and column for uniformity (a gutter is uniform along its length, artwork is not), plans a shot list of pans and zooms, and resolves that plan to exact rectangles at any millisecond; the Control Center's /video page records it to a real .mp4 in the browser with no account, no key and no upload. Verified end to end in headless Chromium against the CEO's own six-panel image: 6 panels found in a 3x2 grid, 8 shots, 16.7s, 1440x1416 MP4 of 7.3 MB that decodes and contains real frames. 62 unit tests. One real defect was found and fixed by that verification: the first cut cropped near-square comic panels to 16:9 and deleted the artist's caption bars, and the fix (contain-by-default framing) exposed a second bug where the renderer stretched instead of padding -- both now have regression tests that fail without the fix. Generated motion (a video model that animates the picture) is NOT built: the page names it, says what it would take, and shows no button for it.",
      "done": true
    },
    {
      "title": "Video Studio, second pass: the page told the CEO that generated motion needs a paid account, which is false and false in the expensive direction. Corrected: the provider panel now names the route that is genuinely our own (a freely licensed open model -- Wan, LTX -- on hardware we own, nothing metered, no image leaving our systems), states what every route costs in words about money, and is ordered cheapest-first. Status renamed needs-account -> not-built, because what is missing is the connection code, not the money. The console also now answers 'do I have an NVIDIA card?' itself, on the Video Studio page, rather than sending anyone to Device Manager -- it runs nvidia-smi, falls back to the Windows display-adapter list, distinguishes 'no card' from 'card with a missing driver', maps video memory to which models are actually runnable, and shows what it ran. The Windows branch cannot execute on the machine this was written on, so the whole decision is injectable and proved by five injected-probe tests; a mutation check confirmed they fail without the platform guard. Also fixed two shipped rendering defects found by reading the served HTML rather than the source: a sentence template that spliced a provider name into nonsense, and this toolchain dropping the space after a closing inline tag, which shipped the word 'notautomatically'.",
      "done": true
    },
    {
      "title": "The console can now RECEIVE work, not just send it. Until now the launcher could start the console but never update it, so once a copy of this repository was on the CEO's machine there was no way for finished work to reach him short of typing git commands -- the exact chore the operating contract forbids, and a hole left by every previous session including the one that wrote the launcher. scripts/update.mjs now runs on every start: it collects whatever has been finished since last time, reinstalls libraries if they changed, rebuilds if the code changed, then starts. It never destroys work (a folder with unsaved changes is left alone), never rewrites history (fast-forward only; a diverged folder stops and says so), never blocks startup (no network means one line and carry on), and never hangs on a credential prompt (GIT_TERMINAL_PROMPT=0, so a private repo fails in seconds instead of freezing the launcher). Proved by scripts/local-test/verify-update.sh against real throwaway git repositories -- 7 git cases and 4 launcher-decision cases, all passing. One real batch defect was caught and fixed in the writing: `if A if B (X) else (Y)` binds the else to the inner if, so the first draft would have skipped the build entirely on a fresh clone and started a console that had never been built.",
      "done": true
    },
    {
      "title": "Shop Transformation Analysis Tool (Phase 1) + BarberOS capability spine (Phase 3). Two things the spec asked for were deliberately NOT built as specified, and both are corrections rather than shortcuts. The spec's `audit_shops` table would have been a second prospect store: visibility.prospects (migration 0017) already is 'a business the agency tenant is selling to', with the converted_tenant_id field that IS the Tool-to-BarberOS handoff, so transform_audit.shop_profiles is a satellite on it carrying only the local-business facts prospects lacks. The spec's `barberos_tenants` would have been the platform's THIRD tenant model, which the current-state audit records the cost of; a shop is a platform.tenants row and barberos.shops is a satellite. The spec also called the audit data 'tenant-agnostic', which non-negotiable #1 does not allow -- it is Herman Legacy Digital's own sales data, so it is scoped to the agency tenant, which also means a second agency can run audits later without a rewrite. The honesty rules the spec states in prose are enforced as constraints: a dimension cannot be scored 'verified' unless a finding on that run carries a real evidence URL; an 'unknown' dimension cannot carry a score and a scored one cannot be called unknown; the composite is derived and a trigger refuses to let anyone write it by hand; it is weighted only over the dimensions actually scored, and the run records how many of the possible dimensions that was so a report states its own coverage instead of implying full coverage; findings are append-only; the outreach hook must cite a finding of its own run; and a recommendation's capability_key is a foreign key, so no report can recommend a module that does not exist. On the BarberOS side, payments cannot be enabled at all (status 'deferred', refused by the enable path -- the spec's deferral made structural), the review engine's config refuses every review-gating key (non-negotiable #5, in the schema rather than in prose), and a capability cannot be enabled before its prerequisites or disabled out from under a dependent. The barbershop website rubric composes the EXISTING scanner (_shared/discovery: SSRF validation, redirect-hop checking, deterministic extraction) and adds only the barber-specific reading of those same facts. A check with nothing to judge is dropped from the denominator rather than scored zero, so a shop is never marked down for something we failed to look at. 'No website' and 'site unreachable' are kept apart: the first is a scored finding of absence at inferred confidence, the second carries no score at all and surfaces as a gap. One real defect was found by its own test and fixed: the booking detector matched the sentence 'Call 716-555-0142 to make an appointment' and scored the shop as having online booking, when that sentence describes the exact opposite. Booking now requires a CTA attached to something clickable, and a tel:/mailto: link never counts. 1,005 pgTAP assertions and 145 edge-function tests, 0 failing, run from an empty database. NOT APPLIED to any environment, no shop imported, no audit run against a real site.",
      "done": true
    },
    {
      "title": "Applied 0048/0049 to canonical production under CEO approval, and found two real defects doing it. FIRST: hand-transcribing the migration into the apply call changed one word of a seeded capability description ('Direct output' for 'Direct answer'). Caught by comparing production against the repo-applied local build rather than by rereading what I typed, and corrected in place. That is exactly the repo-vs-production drift ADR-0002 exists to catch, and it argues for a scripted apply path instead of a transcribed one. SECOND, and the more serious one: five format guards were written the way every HL-BOS migration writes them -- `check (key ~ '^[a-z][a-z0-9_]{2,63}$')` on a citext column. That expression does not mean the same thing everywhere, because `~` is resolved from the search_path in force when the constraint is PARSED. Production's search_path includes `extensions`, so the citext operator resolves and the match is case-INSENSITIVE: 'Booking_Upper' PASSES a guard that reads as 'lowercase only'. The local sandbox's search_path does not, so the same guard is case-SENSITIVE there and the pgTAP suite proved protection production did not have. Verified by direct probe in both environments, not inferred. Migration 0050 casts the five guards explicitly -- four to text (case-sensitive, the intended lowercase-snake-case rule) and capability_requires_not_self to lower() (case-insensitive, which is the CORRECT reading for citext foreign keys onto a citext primary key, so production was right there and the sandbox was wrong). Four new pgTAP assertions test the VALUES rather than the constraint text, so they mean the same thing under either runner; a mutation check confirmed the self-requirement test accepts 'booking' requiring 'BOOKING' without the fix and refuses it with. The same shape exists on 35 further CHECK constraints across 14 other schemas, including vault-reference format guards in ai, billing, comms, integrations and social. Those are PRE-EXISTING and deployed; they are named in the session report for a separate owner decision rather than rewritten unannounced. Behaviour driven by the citext TYPE rather than by operator lookup -- unique indexes in particular -- was confirmed identical in both environments, so the import path's idempotency never diverged. Function bodies are unaffected: all 24 set search_path='' and are therefore consistently text-based in both. Post-apply state, read from production: 9 capabilities, 0 shipped, 0 enabled anywhere, 0 shops, 0 audit shops, 0 runs, 0 findings, 7 new permissions. Advisors 36 -> 36.",
      "done": true
    },
    {
      "title": "The analysis runner, and two more defects it exposed. _shared/transform_audit/run.ts is the piece between the rubric and the database: it owns the ORDER the schema requires (findings before the dimension score, because a dimension cannot claim 'verified' until its evidence exists; recommendations and the hook after the findings they cite). Fetching, DNS and every write are injected, so the same code runs wherever the network actually is -- the Control Center on the CEO's machine, or an edge function -- and it does not choose. Batches are sequential with a delay between shops on purpose: a burst of parallel fetches at fifty small businesses from one address is what makes a crawler look like an attack. Running it end to end for the first time found two things no unit test had. FIRST: a shop whose site refused the connection came back status 'completed'. The report underneath was honest -- coverage said 0 of 1, the finding said the site could not be read -- but a portfolio list shows the STATUS, and 'completed' next to six shops nobody managed to look at is the dashboard lying while the detail page tells the truth. Migration 0051 redefines 'completed' as every weighted dimension actually ASSESSED; a dimension recorded 'unknown' is one we could not reach, so a run carrying one is partially_completed. SECOND: report() ordered recommendations by the priority enum, whose declaration order is ('quick_win','structural'), so every report led with 'add a viewport tag' and buried 'customers cannot book you' -- inverting the analysis's own ranking and contradicting the outreach hook built from it. Now ordered structural-first explicitly, with a test whose structural row is inserted last and ranked ninth so only the explicit ordering can put it first. Proof, against the real schema and the real SQL functions with fixture pages (scripts/local-test/audit-e2e.mts): six shops audited, 70 findings, 21 recommendations, 6 dimension scores stored. Scores ran 100 (owned domain, booking, hours, prices, map, reviews) down to 17, with a booking-platform-hosted shop at 80 correctly passing booking and failing ownership, a shop with no site at 0/inferred, and an unreachable shop at no score/unknown/partially_completed. 11 new runner tests plus the existing 15 rubric tests. NOT run against a real shop website: this container's egress policy denies arbitrary hosts.",
      "done": true
    },
    {
      "title": "The real list, imported and analysed. 50 WNY barbershops into production under the Herman Legacy Digital tenant, every row traceable to its sheet row, and 40 complete analyses computed by the actual runner. The list drove a real correction. 40 of the 50 rows carry a research note rather than a URL, and five of those read 'Booksy presence; dedicated site not confirmed' or similar. Treating them like the other 35 would have put 'there is no way to book you online' in front of shops that are already bookable -- false, and the fastest way to lose the call. classifyWebPresence() now reads the note into three kinds, and a platform-only shop gets a DIFFERENT analysis: the missing owned domain as a failing finding, booking recorded as a PASS, no score at all (two facts from a note are not an assessment), and a hook about ownership rather than bookability. It never recommends the booking module to a shop that can already be booked. A second wording defect was caught before it reached production: the stored dimension note read '0 of 0 checks were evaluable on this page' for shops that have no page. Each no-page case now writes its own sentence, because the note is what a reader sees next to a blank score. Method: import-prospects.mts parses the workbook by header name (no new npm dependency -- it shells to openpyxl, which a one-off spreadsheet read does not justify adding a package for). The runner produced exactly 6 distinct analyses across the 40 shops, so the replay stores six templates and a row-to-template map instead of 40 copies. Both statements were dry-run against a local PostgreSQL 17.6 on the same migrations and verified by fingerprint before and after: import e895b16ca28e005f63a8428c3a31e79c, analyses 51e1c68cfcfc5dece015d027484b6ff7, identical local and production. The exact statements are kept in scripts/local-test/. Every write went through the same permission-checked SECURITY DEFINER functions a signed-in user would call; nothing was inserted around them. Documented in docs/operations/wny-barbers-50-run.md, including what this is NOT.",
      "done": true
    },
    {
      "title": "The Shop Analysis page, and a bug only a rendered page could show. The console now reads the campaign and every shop's full analysis out of HL-BOS Core, and can do the one thing nothing else can: open the websites of shops nobody has visited, from the machine the CEO is sitting at. The build environment's egress policy denies arbitrary hosts, so this capability could never have been delivered as a cloud job. Architecture: the page reads through the Supabase Management API's SQL endpoint using the token the console already holds -- no second credential, and no need to expose transform_audit over PostgREST, which would have meant new public RPCs and a new externally-callable surface. That endpoint connects as postgres, so every WRITE impersonates the tenant owner and goes through the same permission-checked SECURITY DEFINER functions the app uses; scripts/lib/hlbos-sql.mts owns that impersonation and refuses to run at all if the tenant has no owner to act as, rather than quietly writing as a superuser. The fetching runs as a child process, like every other slow thing the console does, one request per shop with a pause between them and an honest User-Agent. THE BUG: the shop list described five shops as \"could not be read\". Nobody had ever tried to read them -- they are the platform-only shops whose entire analysis came from the spreadsheet. Both cases have no score and 'unknown' confidence, so the page had collapsed two different sales situations into one false sentence. The distinction is now carried in the data (does the run have a website_unreachable finding?) rather than guessed at render time, with four tests, including one that a missing column must never be read as failure. It was found by pointing the console at a local PostgreSQL through a stand-in API and looking at fifty real shops on screen -- type checks and unit tests had all passed. That harness is kept (scripts/local-test/supabase-api-shim.cjs) so the page stays checkable. Proved: the audit script end to end against the real schema (10 shops, scores 100 down to 17, one dead site correctly recorded as unreachable with no score); the importer's write path (identical fingerprint to the earlier import, and idempotent on re-import); the page's SQL against the real schema; 48 unit tests on the mapping; the app builds and both pages render, connected and not. NOT proved: the HTTP call to the real Management API, because this environment has no Supabase token. Both runners accept either documented response shape and raise on anything else, so a wrong guess surfaces as a stated error on the page rather than as an empty list.",
      "done": true
    },
    {
      "title": "owned_website, the module the audit actually asks for. All 40 analysed WNY shops point at it and nothing else -- 35 with no website at all, 5 bookable only on a platform they do not own -- so it is the first BarberOS capability to come through the gate 0048 shipped empty. The gate is now load-bearing rather than advisory: barberos.require_capability() is a trigger on every table in the module, so turning the capability off does not hide a shop's page behind an `if` in an application, it makes the rows unwritable at the source. Written generically, with the capability key as a trigger argument, so every future module attaches the same function. The module maps one-for-one onto the audit's failing findings: published_hours, services_listed, pricing_published, address_or_map, tap_to_call, online_booking, social_linked. A shop fixes its audit by filling this in, and the next audit reads the new page and says so. NOTHING IS INVENTED, which is the whole design. There are no defaults for hours, prices or copy. A day with NO ROW means the shop has not said; a row with is_closed means the shop says it is closed -- the schema keeps them apart so the renderer can, because rendering silence as 'Closed' sends a customer away on a day the shop is open, and the SHOP takes that review. A null price renders 'Price on request'; a zero price is refused outright, because it would advertise a free haircut. A page cannot be PUBLISHED until it carries a headline, an address, a way to act and at least one stated day -- a blank page under a shop's own name is worse than no page. No photo gallery in v1: storage_meta.files would be the right home but nothing can upload yet, and a table nobody can fill is the empty-capability liability CONTRIBUTING warns about. THREE DEFECTS FOUND AND FIXED IN THE BUILDING. (1) The publish gate appended to a text[] without a cast, so `text[] || 'a headline'` resolved to array-concat-ARRAY and raised 22P02 'malformed array literal' instead of the sentence it had carefully written. It still blocked publishing -- but a guard that fires with the wrong reason teaches whoever hits it to distrust the guard. (2) The migration claimed a manager could edit the page; 0048 had granted managers read-only. Rather than paper over it, editing page CONTENT got its own permission (barberos.site.update, granted to manager) separate from publishing (barberos.publication.manage, owner and admin only): putting this week's prices up is the daily work of running a shop, not a commercial decision. (3) The gate tests were passing on the SELECT-only grant rather than on the trigger -- same SQLSTATE, so they would have passed with no gate at all. They now run as the owner and match the trigger's own message. One test failed on purpose: 48's `t_no_capability_has_shipped_yet` asserted zero shipped modules and carried a comment saying that failing would be its job. It failed, and it was. Proved end to end against the real schema and the real permission-checked functions (scripts/local-test/barberos-site-e2e.mts): Truth Barbershop -- a real row 4 of the list, on a GlossGenius page it does not own -- gets a page with seven days of hours, five services, prices, a tap-to-call number, a map link and a booking link, published through the gate, then rendered to 4,458 bytes of HTML from what came back OUT of the database. A second shop with an empty page was refused publication, and renders as a page that says it is empty rather than a page of filler.",
      "done": true
    },
    {
      "title": "0052 applied to HL-BOS Core, fingerprint-verified. The gate is now load-bearing in production, not just in a test database: four capability triggers are attached, owned_website is 'available' at version 2, and it is the only capability in the catalog that is. The apply went through the Management API, which means the SQL was TRANSCRIBED rather than shipped -- ADR-0002's checksum lock does not cross that boundary, and an earlier apply in this series drifted by one word in a seed string. So the check is not 'it succeeded': the same normalized fingerprint was computed on the local mirror and on production and compared. It differed on first comparison, in exactly one component (functions). Chasing it down: all nine function bodies were byte-identical; the difference was pg_get_function_identity_arguments rendering upsert_site's citext parameter as 'citext' in production (where extensions is on the search_path) and 'extensions.citext' locally. Same type, different printing -- the same search_path-dependent citext behaviour that produced migration 0050. Normalized for that, both sides give a0c31416669a57cc0e7a4fd542a7f62d. Advisors re-run after the apply: 36 findings, the same 36 as before, none of them in barberos -- the new tables all carry policies, and the new SECURITY DEFINER functions are not in an exposed API schema. Production state confirmed by query: 10 barberos tables, 4 gate triggers, 5 barberos permissions, 1 available capability, 0 sites, 0 tenant capability rows. Nothing was written to production beyond the schema -- no shop, no page, no invented row.",
      "done": true
    },
    {
      "title": "Hosting: the pages are actually served. Until now barberos.site_content() returned a page and site_render.ts turned it into HTML that nothing served -- a published row nobody could read. Two pieces close that. MIGRATION 0053 is the public read path, and it is the first thing in this platform an unauthenticated stranger can call, so it is built as narrowly as it can be: barberos.published_site(slug) returns content only for status='published', takes a slug rather than a tenant_id and returns no tenant_id, and answers NULL identically for a draft, an unpublished page and a slug nobody has used -- so a wordlist cannot reveal which shops have pages they have not published. THE ONE REAL DESIGN ARGUMENT, AND I CHANGED MY MIND MID-BUILD: the public.* wrapper PostgREST can see was written SECURITY INVOKER first, because a wrapper holding no privilege of its own looked tidier and stays out of the advisory the existing bti_*/graph_* wrappers all appear in. Then the pgTAP run failed with 'permission denied for schema barberos' and made the cost explicit: an INVOKER wrapper runs as anon, so anon would need USAGE on the whole barberos schema, and the public internet's reach into it would then be bounded by every function in that schema having remembered to revoke the EXECUTE PostgreSQL grants to PUBLIC by default. Counted: 5 of 19 barberos functions still carry that default. They are trigger functions, so nothing is exposed today -- but 'nothing, provided every future migration remembers' is an invariant nobody is checking. SECURITY DEFINER buys the opposite: anon gets no schema-level reach at all and the door is exactly two functions wide. The tidier version was the less safe one, and the test asserts the trade is real (anon has no USAGE on barberos, and cannot call the read by its own name). THE FUNCTION: supabase/functions/site, verify_jwt=false for this function only, and with no third-party dependency in its serving path -- one hand-written fetch to one RPC with the ANON key, never the service role, with no write reachable at any depth. It serves the page, 301-redirects a capitalised URL to the one real address rather than serving a duplicate, refuses any method but GET and HEAD, answers 304 on an unchanged ETag, returns a CSP of almost entirely 'none' (the rendered page has no scripts and loads nothing external), and turns a database failure into a 503 rather than printing the error onto the shop's own page. PROVED, not asserted: 23 Deno tests on routing and headers, plus scripts/local-test/site-serve-e2e.mjs -- the real function under Deno.serve, over real HTTP, against the real PostgreSQL through a PostgREST-shaped shim that runs every call AS THE anon ROLE so the grants are exercised rather than assumed. 25 checks, all passing: Truth Barbershop's page served in 4,199 bytes with its real hours, prices, tap-to-call and booking link; the draft and an unused slug returning byte-identical 404s; robots and sitemap; and the two that prove publishing means something on the wire -- unpublishing takes the URL off the internet, republishing brings it back. NOT DONE: 0053 is applied nowhere and the function is deployed nowhere, so nothing is reachable on the internet yet; and the address is a Supabase URL, not a domain a shop owns, which is the audit finding this module exists to answer.",
      "done": true
    },
    {
      "title": "0053 applied and the site function deployed -- and deploying it found two bugs and one platform limit that no amount of local testing would have. THE APPLY went first time: the fingerprint over the four new functions, their grants, their comments and the actual reachability of anon (has_schema_privilege on barberos = false, execute on the wrapper = true, execute on the inner function = false) was identical local and production, 647ace45300930371f6d68e6b0b0b157. Advisors 36 -> 40: I predicted 38 and was wrong about the arithmetic, not the cause -- each of the two wrappers trips both the anon and the authenticated SECURITY DEFINER lint, so two functions produce four findings. Every one names one of those two. THE DEPLOY was version 1, 2, then 3. Version 1 was live and answering, and its robots.txt advertised the sitemap at https://edge-runtime.supabase.com/site/sitemap.xml -- a host we do not own. Inside Supabase's edge runtime x-forwarded-host is the internal runtime host, so the function had been sniffing an address that was never public. The same bug sent the 301 redirect there, which is why a capitalised URL answered 401 INVALID_DENO_SUBHOST instead of the shop's page. Fixed by making the public address CONFIGURATION (SITE_PUBLIC_BASE, defaulting to the project's own API URL plus the function path) rather than something to infer. THE FIRST FIX WAS INCOMPLETE, and version 2 proved it: I made the redirect relative, which fixes the host but not the path, because the gateway also strips /functions/v1 before the function sees the URL -- so it redirected to /site/x, which the gateway rejects with 'requested path is invalid'. The request knows neither its host nor its prefix. Version 3 builds the redirect from the configured base's path and is verified correct on the real URL: Location: /functions/v1/site/truth-barbershop, followed through to our own 404 page because no such page exists yet. Four new tests cover both bugs, including one that asserts the configured base beats anything in the request. THE PLATFORM LIMIT, which is the important finding: Supabase's gateway rewrote our content-type and our CSP on exactly the two responses whose bodies were HTML and XML (to text/plain and 'default-src none; sandbox') while leaving the plain-text robots.txt with our exact headers. That is content-type-driven sandboxing of active content on the shared *.supabase.co domain, observed rather than assumed. It means a shop's page on that domain renders in a browser as its own source code. The serving layer is right; the domain is now a functional requirement, not a presentation preference. NOT VERIFIED FROM PRODUCTION: the rendered page itself, because no shop has published one. Everything about it is covered locally end to end against a real PostgreSQL as role anon; nothing about it has been served from HL-BOS Core.",
      "done": true
    },
    {
      "title": "The domain question, answered against the vendor's own documentation rather than by buying the obvious thing. Supabase states plainly that Edge Functions rewrite text/html to text/plain and that serving HTML needs a custom domain -- so the previous session's observation is now confirmed rather than inferred. The obvious move is to buy the custom-domain add-on and point a subdomain at HL-BOS Core. It is the wrong move, and the same documentation says why: a Supabase project accepts exactly ONE custom domain, ever. `owned_domain` is a failing audit finding for five of the fifty WNY shops and is the finding this module exists to answer, so a route that structurally permits one domain for the entire platform would fix today's symptom and permanently foreclose the goal. Two more from the same page: only subdomains are supported, so hermanlegacydigital.com itself cannot be used at all; and activating a custom domain moves the platform's Auth hostname -- OAuth callbacks and the SAML EntityID -- onto a customer-facing marketing domain, which is a bad thing to weld together for a $10/month add-on. DECISION: shop pages get served by a web host, not by Supabase. The page server was already built portable -- site_server.ts and site_render.ts have no Supabase dependency and site_source_rest.ts is the only file that talks to the database, over plain HTTP with the anon key -- so this is a deployment change, not a rewrite. It buys clean URLs, our own content-type, MANY domains so a shop can eventually own one, an untouched Auth hostname, and no add-on. The Supabase deployment stays: it is the same handler, it proved the whole anon database path works in production, and it is a correct API-shaped endpoint -- just not where customer-facing HTML belongs. BUILT THIS SESSION: the console now probes the live address on every render of the Shop Analysis page and says in plain English whether a customer would get a page or a wall of source code. It asks for a slug nothing is published at, so it works today, before any shop has a page -- which is exactly when a status panel is most tempting to fake. 11 tests, including one that no unrecognised response shape is ever read as success. STILL BLOCKED, and only on access: a host account and one CNAME on a subdomain.",
      "done": true
    },
    {
      "title": "apps/shop-pages: the same server, on a host that can serve HTML. Coolify already exists, which removes the whole vendor question -- no new account, no DNS migration, no $10/month add-on, and a host that can hold as many domains as we like, which is what eventually lets a SHOP own its page's domain (the audit finding this module answers and the one thing a Supabase custom domain could never do, since a project accepts exactly one). THE KEY DECISION: this is not a port or a copy. server.ts imports handle() from supabase/functions/_shared/barberos/site_server.ts, so routing, headers, caching, the lowercase redirect and the 404-that-hides-drafts have ONE implementation and ONE set of tests, and the Supabase deployment and this one cannot drift. Only two things differ, both consequences of the host: pages at /<slug> rather than /functions/v1/site/<slug>, and a content type nothing rewrites. SITE_PUBLIC_BASE is REQUIRED here rather than defaulted -- on Supabase there is a correct default (the project's own API URL); on a general host a guess would put the wrong hostname into robots.txt and the sitemap, which is exactly the bug the first deployment produced. The container is pinned to Deno 2.9.6, type-checks at BUILD time so a mistake fails the build rather than the first customer's request, runs as non-root with --allow-net and an explicit --allow-env allowlist, and copies only the four files it needs rather than the repository. PROVED: the end-to-end now starts this exact entrypoint over real HTTP against the real PostgreSQL through a shim that executes every call as the anon role -- a page at the root byte-identical to the other deployment's, served as real HTML with our own CSP intact, a sitemap of clean root URLs, a redirect with no path prefix at all, and a draft still invisible. 35 checks, all passing. The governance tests caught what I would have missed twice over: a new app under apps/ that is in neither the application registry nor the catalog fails the completeness check. Both registered. NOT DONE: nothing is deployed to Coolify, no subdomain points at it, and the Docker image has never been built -- there is no Docker in this environment, so the Dockerfile is the one part of this that is written and not run.",
      "done": true
    },
    {
      "title": "The address is decided: shops.hermanlegacydigital.com, on the existing Coolify server. One CNAME on that subdomain; nothing else on the domain is touched and the apex stays free for the agency's own site. A subdomain rather than the apex is not a platform constraint any more -- that stopped mattering when the pages left Supabase -- it is just right: the agency's front door and its customers' pages are different things and should not share a hostname. The address is a CONSTANT in the console, not a setting, because it is a fact about the business: if it changes, the change belongs in a reviewed commit beside the ADR that explains it rather than in an untracked file on one machine. A machine-local override remains for pointing the console at a staging deployment, and the panel says when one is in force. The console now checks that address on every render of the Shop Analysis page, and it CURRENTLY FAILS, because nothing is deployed there yet -- which is the intended behaviour. A status panel that checks something easier in order to look green is the exact failure the honesty rules exist to prevent, so the probe reports the real address and says plainly that nothing is answering at it. Placeholders removed everywhere: the README, both registries and the console portfolio entry now carry the real address and the real project URL instead of example.com.",
      "done": true
    },
    {
      "title": "Deployed, and the blocker is gone. apps/shop-pages is live at shops.hermanlegacydigital.com on the Coolify server, built from commit a5dc6cd. The pinned denoland/deno:alpine-2.9.6 image -- the one part of this written and never run, because there is no Docker in the build environment -- resolved and built first time. PROVED FROM PRODUCTION, via pg_net, because this environment cannot reach arbitrary hosts: /robots.txt 200 text/plain with 'Sitemap: https://shops.hermanlegacydigital.com/sitemap.xml' (the configured base, no trace of the edge-runtime.supabase.com bug the first deployment produced); /sitemap.xml 200 application/xml; and a slug with no page 404 as text/html. That last one is the entire reason for the move: the IDENTICAL response from the IDENTICAL handler came back from Supabase as text/plain with 'content-security-policy: default-src none; sandbox', which is a browser showing a shop its own source code. Here it is real HTML carrying our own policy -- and every response kept our CSP, which the Supabase gateway had replaced on exactly the HTML and XML ones. TLS issued by Let's Encrypt with no intervention; pg_net verifies certificates and all three requests succeeded. A CORRECTION WORTH KEEPING: the DNS is an A RECORD, not a CNAME. I said CNAME earlier and that was wrong -- the Coolify host is a bare IP and a CNAME cannot point at an address. The apex already pointed at the same box, so the subdomain inherited a working reverse proxy and resolved almost immediately. In passing: the apex sends HSTS with includeSubDomains, so before the certificate existed Chrome refused to offer any click-through on the subdomain. Correct behaviour, and it resolved itself. STILL NOT TRUE: no shop has the capability enabled, none has published a page, the sitemap is an empty urlset, and nobody has read a real shop page. Everything between the database and the browser is proven; what is missing is a customer.",
      "done": true
    },
    {
      "title": "Three looks for a shop page, because the first one was not sellable. Keith's words: 'that's a shitty website'. He was right. The renderer had been optimised for three constraints -- never invent content, load nothing external, no photographs (nothing can upload yet) -- and together they produce something truthful that reads like a utility bill. Barbers are bought on how they look; a page that looks like a spreadsheet undersells the shop. PLAIN AND HONEST TURNED OUT NOT TO BE THE SAME THING: a page can be striking and still refuse to invent a single price, and they were only ever in tension because they were built in the wrong order. site_themes.ts supplies CSS AND NOTHING ELSE -- three directions: Chair (dark, editorial, warm, the default), Fade (bright, modern, high contrast) and Pole (vintage americana). The separation is asserted rather than trusted: strip the stylesheet and all three produce BYTE-IDENTICAL markup apart from the theme name on <body>, so no amount of styling can add a word the shop did not put in the database. A second test proves they are genuinely different stylesheets rather than one wearing three names, which would make the choice a lie. A third forbids @import, url(http, //fonts. and https:// in any theme, because the page is served with default-src 'none' and a web font would be silently blocked and look broken to the shop. An unknown theme key falls back rather than throwing -- a configuration mistake must not take a shop's page off the internet. 211 edge-function tests. TWO REAL CEILINGS ON HOW FAR THIS GOES, both named in the file: no web fonts without loosening the policy that makes the page inert, and no photography until there is an upload path -- a great barbershop site is largely photography, and typography alone is not the same thing.",
      "done": true
    },
    {
      "title": "The discovery call, which is where the other half of the picture actually comes from. Keith settled a question several sessions had been circling: what the business already runs is learned on the introductory phone call, not inferred from outside. That makes the sale two stages and gives the platform a clean join between them -- the AUDIT produces the opener (what anyone can see), the CALL produces the proposal (what only the owner can tell us). Migration 0054 adds transform_audit.discovery, a satellite on visibility.prospects like shop_profiles, holding what the shop said it runs. EVERY ANSWER IS THREE-STATE AND THAT IS THE ENTIRE POINT: true means they have it, false means we asked and they do not, NULL means NOBODY ASKED. Collapsing the last two is how a proposal ends up telling an owner they have no online booking when nobody raised it -- and how you get corrected in front of their staff. It is the same rule the audit already enforces for scores, applied to the half that comes from a conversation instead of a crawler, and capability_match.ts reads these columns as boolean|null for exactly that reason. A partial call updates only what was discussed: an absent JSON key keeps the stored answer, so a second call about the phones does not wipe what the first learned about booking; an explicit null CLEARS an answer back to 'nobody asked', for whoever writes down the wrong shop. A call that established nothing does not restamp the timestamp claiming it did. Provenance is enforced -- an answer with a time but no person is refused, because an answer with nobody behind it is a rumour. One contradiction is caught at the schema while the owner is still on the phone to correct it: a named booking platform cannot coexist with 'no online booking'. 28 pgTAP assertions, and THREE OF THEM WERE WRONG FIRST TIME in ways worth keeping: looking the prospect up by name returned NULL once signed in as someone RLS hides it from, so three permission guards were being handed a NULL id and proving nothing while appearing to pass -- fixed with a fixed UUID; and an assertion that a timestamp was 'earlier than now()' could never be true, because now() is frozen for the whole transaction. 1,113 pgTAP assertions, 0 failing. NOT APPLIED anywhere -- 0054 awaits approval.",
      "done": true
    },
    {
      "title": "0054 APPLIED to HL-BOS Core, and the discovery call is a screen you can use on the phone. THE APPLY was fingerprint-identical local and production on the first comparison -- f015d345b71fac28f0ae26937a502af1 over transform_audit.discovery's columns, its four constraints, its policies, its trigger, its grants, both new functions and the new permission row. No fixing up, no second pass. THE SCREEN: /shops/<id>/call. Questions down the left, the opportunity assembling on the right as they are answered, because it is meant to be open WHILE THE SHOP IS ON THE PHONE. Every question has THREE buttons -- Yes / No / Not asked -- not a checkbox, because a checkbox forces every unasked question to read as 'no', and 'no' is a claim. Nothing saves until Save is pressed, so a misheard answer can be corrected without a round trip mid-sentence, and ONLY THE QUESTIONS ACTUALLY TOUCHED ARE SENT, so a second call about the phones does not wipe what the first learned about booking. THE WRITE GOES THROUGH THE FRONT DOOR: the Management API's SQL endpoint connects as postgres, which bypasses RLS and every permission check in the schema, so recordCall assumes the tenant owner's identity first (set local role authenticated) and REFUSES OUTRIGHT if the tenant has no active owner to act as -- the console must not be able to store what the application itself would refuse. A CORRECTION: capability_match.ts had been written into supabase/functions/_shared, which is Deno territory the Next app cannot reach into -- the cross-reference was sitting where the one screen that needs it could not import it. Moved to apps/control-center/src/lib/capability-match.ts and its 19 tests ported to vitest. PROVED: 1,113 pgTAP assertions and 0 failing on a database rebuilt from all 54 migrations; 842 unit tests; 211 edge-function tests; and scripts/local-test/discovery-call-e2e.mts -- 19 checks against the real PostgreSQL, writing as the owner through the real permission path, proving a partial call adds rather than overwrites, an answer can be explicitly un-said, and a false survives as false while a null survives as null. THE HONEST RESULT OF THE CROSS-REFERENCE, which is the number that matters: for a shop that answers everything, the catalog yields EXACTLY ONE thing deliverable today -- Owned Website / Landing Page -- against five on the roadmap. That is not a defect in the matcher; it is that eight of the nine BarberOS modules are unbuilt, and the screen says so in those words rather than blurring the two lists. NOT BUILT: the SEO/funnel/agency half of the offer has no representation in the catalog at all; there is no economics model behind 'recovers', so the screen names what is lost without pricing it; and no proposal document comes out the other end yet -- the call produces a matched list on screen, not an artefact you can send a shop.",
      "done": true
    }
  ],
  "blockers": [
    {
      "title": "Accounts not connected yet",
      "meaning": "Open Connect accounts in the console and paste a GitHub token and a Supabase token. Two minutes, once. Until then the console cannot show your builds or the database.",
      "owner": "ceo"
    },
    {
      "title": "Deploy buttons are not built yet",
      "meaning": "Deploying needs a preview environment to test against, and Supabase branching is not switched on. Building a Deploy button before then would be a button that cannot deploy.",
      "owner": "ai-engineer"
    },
    {
      "title": "Social channels are not connected yet",
      "meaning": "The publishing software is built and tested, but it has no accounts to publish to. Six things are needed before the first real post: the exact list of Facebook Pages, Instagram accounts, LinkedIn profile and TikTok accounts to use; confirmation each Instagram account is a Professional account linked to a Facebook Page (a personal account cannot post automatically at all); a Meta app with the Instagram and Facebook Login products; a tester/admin role on that app for each account; a LinkedIn app with 'Share on LinkedIn'; and a TikTok app with inbox-only content posting. Each is an access decision only you can make. Everything not waiting on them is finished.",
      "owner": "ceo"
    },
    {
      "title": "Production has drifted from the repository, and the safe apply path is blocked",
      "meaning": "While applying the social migration I checked what the live database actually contains. It holds about forty migrations that are not in this repository at all (three separate naming schemes: dma_*, disco_*, jobscout_*), and around fourteen of our own migrations were applied under different version stamps than the files carry. The registry file that is supposed to track this still says the database is at migration 28; it is far past that. The practical consequence: the protected one-click migration workflow cannot be used safely right now -- its drift check would fail, and if it were forced past that it would try to re-run around twenty migrations the database already has under other names, including two data backfills. That is why 0046 and 0047 were applied individually and verified rather than pushed as a batch. Reconciling this properly is its own piece of work and needs your go-ahead; it is not urgent, but it should not be left indefinitely.",
      "owner": "ai-engineer"
    }
  ]
}
